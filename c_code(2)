#include <stdio.h>
int main() 
{
int n;
char c,str[50];
double d;
printf("enter the character:");
scanf("%c",&c);
printf("the entered character is :%c", c);
printf("\n enter the double:");
scanf("%d", &d);
printf("the entered double is:%d", d);
printf("\n enter the integer number:");
scanf("%d", &n);
printf("the entered number is :%d", n);
printf("\n enter the string:");
scanf("%s", str);
printf("the entered string is:");
puts(str);
return 0;
}
